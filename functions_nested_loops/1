#include "main.h"

/**
 * _abs - prints the absolute value of r.
 * Descritption: this is a function that prints
 * the absolute value of r.
 * @int: this is the value that you enter
 * Return: 0 always.
 */

int _abs(int)
{
	int p;

	p = int * -1;

	if (int < 0)
	{
		_putchar(p);
		return (0);
	}
	else
	{
		_putchar(int);
		return (0);
	}
}
