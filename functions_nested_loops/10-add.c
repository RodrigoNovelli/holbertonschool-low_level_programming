#include "main.h"
/**
 *add - This function makes an add with two variables
 *@a: This is the first number that you choose
 *@b: This is the second number that you must choose
 *Return: Always 0.
 */

int add(int a, int b)
{
	int sum;

	sum = (a) + (b);
	return (sum);
}
